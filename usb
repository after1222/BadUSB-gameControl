#include "Keyboard.h"  

 void setup() {   
  Keyboard.begin();
  delay(500);
  //Keyboard.press(KEY_LEFT_SHIFT);
  //Keyboard.press('w');

  
  for(int i=0;i<=160;i++){
  Keyboard.press('a');
  delay(115);
  Keyboard.release('a');
  Keyboard.press('d');
  delay(115);
  Keyboard.release('d');
  }
  Keyboard.releaseAll();
  }
 void loop() {
 }
